const Background = () => {
  return (
    <section
      id='backdrop'
      className='fixed h-screen hidden bg-black/10 backdrop-blur-sm z-[997] inset-0 w-full'
    ></section>
  );
};

export default Background;
